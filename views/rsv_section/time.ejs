<dl>
  <dt v-for="(time, i) in formData.time_array" :key="i">
      <button type="button" :class="timeBlockClass(time)" @click="clickTimeBlock(time)">
        {{time.start_time}}  ~ {{time.end_time}}
      </button>
  </dt>
</dl>