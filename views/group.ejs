<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
  
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <script type="text/javascript" src="/javascripts/index.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/vue"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
</head>

<body>
  <div id="app">
    <header>
      <% include ./header %>
    </header>

    <main>
      <button type="button" class="btn btn-primary">학과</button>
      <button type="button" class="btn btn-default">{{group.department_name}}</button>
      <br/>
      <button type="button" class="btn btn-primary">학습관</button>
      <button type="button" class="btn btn-default">{{group.building_name}}</button>
      <br/>
      <button type="button" class="btn btn-primary">이름</button>
      <button type="button" class="btn btn-default">{{group.name}}</button>
      <br/>
      <button type="button" class="btn btn-primary">설명</button>
      <button type="button" class="btn btn-default">{{group.description}}</button>
      <br/>
      <button type="button" class="btn btn-primary">생성일</button>
      <button type="button" class="btn btn-default">{{group.date_created}}</button>
      <br/>
      <button type="button" class="btn btn-warning" v-if="group.is_mine==1">수정</button>
      
      <h2>가입한 사람</h2>

      <table class="table table-hover">
        <thead>
          <th>이름</th>
          <th>학번</th>
          <th>전화번호</th>
          <th>가입일</th>
        </thead>
        <tbody>
          <tr v-for="(person, i) in results" :key="i">
            <td>{{person.name}}</td>
            <td>{{person.student_number}}</td>
            <td>{{person.phone}}</td>
            <td>{{person.date_joined}}</td>
          </tr>
        </tbody>
      </table>
    </main>
    
    <footer>
      <% include ./footer %>
    </footer>
  </div>

  <script>
    var global = makeGlobal();
    var group = <%-JSON.stringify(group) %>;
    var results = <%-JSON.stringify(results) %>;

    var app = new Vue({
      el: '#app',
      data: function () {
        return {
          page_name: 'group',
          is_user: <%- is_user %>,
          is_admin: <%- is_admin %>,
          group,
          results,
        }
      },
    })
  </script>

</body>

</html>